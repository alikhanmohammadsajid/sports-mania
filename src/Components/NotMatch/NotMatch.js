import React from 'react';

const NotMatch = () => {
    return (
        <div className="container">
            <h1>404 - File or directory not found.</h1>
            <h5>The resource you are looking for might have been removed, had its name changed, or is temporarily unavailable.</h5>
        </div>
    );
};

export default NotMatch;